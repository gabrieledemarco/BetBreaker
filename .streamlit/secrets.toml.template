# .streamlit/secrets.toml
# ─────────────────────────────────────────────────────────────
# Template per il deploy su Streamlit Cloud.
# NON committare questo file con le chiavi reali.
# Su Streamlit Cloud: Settings → Secrets → incolla il contenuto.
# ─────────────────────────────────────────────────────────────

# Opzionale: solo per parsing quote da screenshot
ANTHROPIC_API_KEY = "sk-ant-..."
